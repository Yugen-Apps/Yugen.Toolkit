<Page
    x:Class="UwpCommunity.Uwp.Samples.Views.Navigation.NavigationParameterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpCommunity.Uwp.Samples.Views.Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:UwpCommunity.Uwp.Samples.ViewModels.Navigation"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="{x:Bind ViewModel.Page_Loaded}">

    <Page.DataContext>
        <viewModels:NavigationParameterViewModel/>
    </Page.DataContext>

    <Grid>
        <TextBlock Text="{x:Bind ViewModel.Text, Mode=OneWay}"/>

        <Button Content="Test" Tapped="{x:Bind ViewModel.Button_Tapped}"/>
    </Grid>
</Page>
