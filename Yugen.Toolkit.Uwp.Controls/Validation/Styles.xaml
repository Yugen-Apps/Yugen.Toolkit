<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
        <!-- Other merged dictionaries here -->
    </ResourceDictionary.MergedDictionaries>

    <!-- Other app resources here -->
    <Color x:Key="ValidatingErrorColor">red</Color>
    <SolidColorBrush x:Key="ValidatingErrorBrush"
                     Color="{StaticResource ValidatingErrorColor}" />

    <Color x:Key="ValidatingSuccessColor">Gray</Color>
    <SolidColorBrush x:Key="ValidatingSuccessBrush"
                     Color="{StaticResource ValidatingSuccessColor}" />

    <Style x:Key="ValidatingTextBlockStyle"
           TargetType="TextBlock">
        <Setter Property="Foreground"
                Value="White" />
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="TextWrapping"
                Value="WrapWholeWords" />
        <Setter Property="FontWeight"
                Value="SemiLight" />
    </Style>
    
</ResourceDictionary>
