<UserControl
    x:Class="UwpCommunity.Uwp.Controls.NotificationBannerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpCommunity.Uwp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:UwpCommunity.Uwp.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Height="40"
    Opacity="0"
    x:Name="NotificationBanner"
    Loaded="NotificationBannerUserControl_OnLoaded">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Storyboard x:Name="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="NotificationBanner"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0"
                             To="1.0"
                             Duration="{x:Bind Duration, Mode=OneWay}"
                             AutoReverse="{x:Bind AutoReverse, Mode=OneWay}"
                             RepeatBehavior="1x" />
        </Storyboard>

        <Style x:Key="NotificationBannerError" TargetType="Grid">
            <Setter Property="Background" Value="Red" />
        </Style>

        <Style x:Key="NotificationBannerWarning" TargetType="Grid">
            <Setter Property="Background" Value="Yellow" />
        </Style>

        <Style x:Key="NotificationBannerValid" TargetType="Grid">
            <Setter Property="Background" Value="Green" />
        </Style>
    </UserControl.Resources>

    <Grid
        Style="{x:Bind UcStyle, Mode=OneWay}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <SymbolIcon Symbol="{x:Bind BannerSymbol, Mode=OneWay}"
                    Margin="10"
                    Grid.Column="0"
                    Grid.RowSpan="2" />
        <TextBlock Text="{x:Bind Message, Mode=OneWay}"
                   Margin="10"
                   Grid.Column="1"
                   Grid.Row="0"
                   Grid.RowSpan="2"
                   VerticalAlignment="Center"
                   Tapped="TextBlock_Tapped" />
        <StackPanel Orientation="Horizontal"
                    Grid.Column="2"
                    Grid.Row="0"
                    HorizontalAlignment="Stretch">
            <Button Background="WhiteSmoke"
                    Visibility="{x:Bind BannerButtonCloseVisibility, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                    Tapped="ClearButton_Tapped">
                <SymbolIcon Symbol="Clear" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
